🔥 R — Hot Restart
What it does
Completely restarts the Flutter app
Re-runs main() from scratch
Clears all in-memory state
When to use
You changed:
app initialization logic
main() method
dependency injection
global variables
Hot Reload doesn’t reflect changes
Effect
App UI reloads
Variables reset to initial values
Example
int counter = 0;
After hot restart → counter becomes 0 again

---------------------------------------------
📖 h — Help (list commands)
What it does
Displays all available interactive commands in terminal
When to use
You forgot a command
You want to explore more options (like s, p, o)
Effect
No change to app
Just prints help text

---------------------------------------------
🔌 d — Detach
What it does
Stops flutter run
App keeps running on device/emulator
When to use
You want to:
free the terminal
attach later using debugger
let app run independently
Effect
Flutter CLI disconnects
App continues normally
⚠️ App will stop only when you manually close it on device.


-----------------------------------------------------------
🧹 c — Clear Screen
What it does
Clears the terminal output
When to use
Terminal is cluttered
You want a clean view for logs
Effect
Purely visual
App keeps running


--------------------------------------------------------
❌ q — Quit
What it does
Completely stops:
Flutter process
App running on device/emulator
When to use
You’re done testing
Want to restart from scratch
Switching projects
Effect
App is terminated
Terminal exits Flutter run mode


***********************************************
🧠 Quick Comparison Table
Key	Action	            App State	        main()reruns
R	Hot Restart	        Resets	            ✅ Yes
h	Help	            No change	        ❌ No
d	Detach	            Keeps running	    ❌ No
c	Clear screen	    Keeps running	    ❌ No
q	Quit	            Stops app	        ❌ No
💡 Pro Tip (Flutter dev habit)

Use Hot Reload (r) → for UI tweaks

Use Hot Restart (R) → for logic/init changes

Use Quit (q) → only when necessary (slower)